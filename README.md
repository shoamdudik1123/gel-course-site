# קורס לק ג'ל – אפק דודיק

אתר דף נחיתה לקורס לק ג'ל עם אפק דודיק.

## מה יש באתר

- **דף ראשי** עם אזור היכרות (Hero) והכפתור "הרשמה לקורס"
- **אודות הקורס** – תיאור קצר + יתרונות (הכשרה מעשית, חומרי לימוד, תעודת סיום)
- **מה נלמד** – רשימת נושאי הקורס
- **המרצה** – תמונה וטקסט על אפק דודיק
- **דף הרשמה** (`register.html`) – טופס עם וולידציה. הפרטים נשלחים למייל (דרך Formspree) ולמספר 0543205622 (וואטסאפ).

## איך להריץ

1. פתח את התיקייה בפרויקט.
2. הפעל שרת מקומי (אופציונלי אבל מומלץ):
   - **VS Code**: התקן את ההרחבה "Live Server" ולחץ "Go Live".
   - **או** פתח ישירות את `index.html` בדפדפן (לחיצה כפולה על הקובץ).

## התאמה אישית

- **תמונת מרצה**: שמור את התמונה של אפק דודיק בתיקייה `images` בשם `afek-dudik.png` (או עדכן את הנתיב ב־`index.html` בשדה `src` של התמונה).
- **וואטסאפ/טלפון**: ערוך את הקטע "הרשמה ופרטים" ב־`index.html` והוסף קישור לוואטסאפ ומספר טלפון.
- **שליחה למייל**: כדי שההרשמות יגיעו ל־shoamdudik1123@gmail.com – עקבו אחרי ההוראות בקובץ **הפעלת שליחה למייל.txt** (הרשמה ב־Formspree והדבקת Form ID ב־register.html).

## קבצים

- `index.html` – הדף הראשי
- `register.html` – דף הרשמה עם טופס וולידציה
- `register.js` – וולידציה ושליחה למייל/וואטסאפ
- `style.css` – עיצוב ו־RTL
- `script.js` – תפריט מובייל וטיפול בתמונה
- `הפעלת שליחה למייל.txt` – הוראות להפעלת שליחה אוטומטית למייל
- `README.md` – הקובץ הזה
